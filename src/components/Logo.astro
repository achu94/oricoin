---
import { Image } from "astro:assets";
import logo from "../assets/logo.png";

interface Props {
  width?: number;
  height?: number;
}

const width = Astro.props.width ?? 42;
const height = Astro.props.height ?? 42;
---

<Image src={logo} alt="Oricoin Logo" width={width} height={height} />
